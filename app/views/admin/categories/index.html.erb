<h1>Admin Categories</h1>

<%= form_for [:admin, @category] do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.submit %>
<% end %>
<br>
<% @categories.each do |category| %>
  <p><%= category.title %>:
<% end %>
